// Types for the app bundle (build/app-bundle.json, produced by engine/build_bundle.py). Loose where
// the corpus is loose - this mirrors the YAML, it does not re-specify it. The corpus is the source of
// truth; these are just enough shape for the engine core to read the bundle safely.
export {};
